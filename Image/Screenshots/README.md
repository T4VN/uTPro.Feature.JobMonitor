# Screenshots

Screenshots of the Background Jobs Monitor dashboard, referenced from `README.md` and
`umbraco-marketplace.json` (via raw GitHub URLs).

Current:

- `dashboard.png` — the jobs table.
- `run-now.png` — the Run now action with populated last-run / telemetry data.

When adding more, reference them from `umbraco-marketplace.json` -> `Screenshots[]` using raw URLs,
e.g. `https://raw.githubusercontent.com/T4VN/uTPro.Feature.JobMonitor/main/Image/Screenshots/<name>.png`.
